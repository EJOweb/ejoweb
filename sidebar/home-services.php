<?php if ( is_active_sidebar( 'home-services' ) ) : // If the sidebar has widgets. ?>

	<?php dynamic_sidebar( 'home-services' ); // Displays the default sidebar. ?>

<?php endif; // End sidebar active check. ?>
